---

- name: Converge
  hosts: all
  vars:
    icinga2_features:
      - name: checker
      - name: mainlog
      - name: api
        ca_host: none
        endpoints:
          - name: "{{ ansible_fqdn }}"
        zones:
          - name: "main"
            endpoints:
              - "{{ ansible_fqdn }}"
    icinga2_config_directories:
      - zones.d/main/hosts
  collections:
    icinga.icinga
  tasks:
    - debug:
        var: ansible_fqdn
        
    - name: "Include Icinga Repos"
      include_role:
        name: repos
    - name: "Include Icinga 2"
      include_role:
        name: icinga2
